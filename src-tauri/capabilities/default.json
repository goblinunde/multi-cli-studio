{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Multi CLI Studio.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "process:default",
    "updater:default"
  ]
}
